// hi there

count = 11;
record = { "Seth", "Rachel"}; // set - values are optional
list = [ 5 ];

reply count + count;

reply ((14 / 37));

// could use << for iterating backwards??
// chained blocks

list >> (num) {

    if (num == 17) {
        remote::mult(num)
    }
}


1..n >> (i) {

}

reply count;