references:

Timer <exa:timer>

main is service {

    receive io, system;

    io.out.write("ready\n");

    Timer:set<1000, 0> then {
        io.out.write("set\n");
    }

    Timer:set<2000, 0> then {
        io.out.write("go!\n");
    }
};
