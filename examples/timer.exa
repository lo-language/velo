references:

Timer   <exa:timer>


main is -> (io, system) {

    receive io, system;

    io.out.write("ready\n");

    Timer:start(1000, 0) -> {
        io.out.write("set\n");
    }

    Timer:start(2000, 0) -> {
        io.out.write("go!\n");
    }
};
