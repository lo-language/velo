/**
 * Hilariously naïve computation of the Ackermann–Péter function
 */

receive m, n;

if m == 0
    reply(n + 1);

if m > 0 and n == 0
    reply(recur(m - 1, 1));

if m > 0 and n > 0
    reply(recur(m - 1, recur(m, n - 1)));