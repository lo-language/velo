receive args, io;

numPoints is 2;

// recursively iterate
// label points as 0-8

num = 0;
count = 0;
path = [];

explore = >>:

    receive start, indent;

    total = 0;

    while start < numPoints:
//        found = explore(start + 1, "`indent`  ");
        total = total + found;
        start++;

    reply total;
;

found = explore(0, "");
io.stdout.write("\n\nfound: `count`\n");