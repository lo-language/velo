/**
 * Created by: spurcell
 * 11/10/13
 */
action () {

    x = action () {};

    #items == 0;

    // this sends bar the value of x = foo
    x = foo -> bar

    // not much to see here
    fail "oh noes!";
}