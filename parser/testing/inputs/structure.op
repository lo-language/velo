/**
 * Created by: spurcell
 * 11/10/13
 */
action () {

    x = action () {};

    if (#items == 0) {
        fail "oh noes!";
    }

    if (#items == 1) {
        x = 2;
    }
    else {
        y = 3;
    }

    // this sends bar the value of x = foo
    x = foo -> bar
}