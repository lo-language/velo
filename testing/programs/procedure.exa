/**
 * Created by: spurcell
 * 2/7/15
 */


foo is:

    receive x;

    factorial is:

        receive num;

        if num <= 1:
            reply num;

        reply num * recur(num - 1);

    reply factorial(x) / 2;


reply foo(5);

