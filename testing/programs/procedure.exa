/**
 * Created by: spurcell
 * 2/7/15
 */


foo is:

    receive x;
    reply factorial(x) / 2;


factorial is:

    receive num;

    again = recur;

    if num <= 1:
        reply num;

    reply num * again(num - 1);


reply foo(5);

