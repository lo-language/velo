/*
 * recursive factorial calculator
 */

main is (num) {

    if !have num {
        fail "c'mon!";
    }

    if num < 0 {
        fail "I pity the fool!";
    }

    if num <= 1 {
        reply 1;
    }

    reply num * main(num - 1);
};
