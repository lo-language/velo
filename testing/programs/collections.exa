/**
 * Created by: spurcell
 * 12/25/14
 */

// Array literal (including empty), access, and cardinality
array = [];
words = ["foo", "mani", "padme", "hum"];

if #array != 0:
    fail "problem";

if words[2] != "padme":
    fail "oh me oh my";

if #words != 4:
    fail "what the deuce?!";

// Map literal (including empty), access, and cardinality
map = [:];
cities = ["nyc":"New York", "phi":"Philadelphia"];

if #map != 0:
    fail "problem";

if cities.nyc != "New York":
    fail "something happened";

if cities["nyc"] != "New York":
    fail "whoops";

if #cities != 2:
    fail "what the deuce?!";


// String cardinality
name = "snooks";

if #name < 6:
    fail "WHAT?!";


//if #dudes < 3:
//    fail "not very froody";

// todo throw an error on undefined identifiers
//if #set != 3
//    fail("surely you"re joking");

// test membership
// or should these be a in b?

//if "om" in words:
//    fail "oops";

//if "trillian" in dudes:
//    fail "not a dude";

// shouldn"t be necessary but is for now; otherwise we never conclude
//reply;

//if #map != 3
//    fail("a most unpleasant situation, captain");
