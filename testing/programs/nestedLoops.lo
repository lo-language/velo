MAX is 3;

main is (log) {

    count = 0;

    y = 0;

    while y < MAX {

        x = 0;

        while x < MAX {

            x++;
            count++;
            log count;
        }

        y++;
    }

    reply count;
};