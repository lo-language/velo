/**
 * Created by: spurcell
 * 2/7/15
 */


factorial is (num) {

    again = factorial;

    if num <= 1 {
        reply num;
    }

    reply num * again(num - 1);
};

foo is (x) {

    reply factorial(x) / 2;
};

main is () {
    reply foo(5);
};