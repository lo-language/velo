Moon is module;
HTTP is module NPM::HTTP;

name is "Bigfoot Bjornsen";

game is getGame();  // todo this is grammatical but should be a compile error

test is () {

    // a set constant

    crew is {

        "Mike Collins"
        "Neil Armstrong"
        "Buzz Aldrin"
    };

    timeStep is env.clock.interval(UPDATE_INTERVAL);

    moon is Moon.create();
};