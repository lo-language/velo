test is <-> {

    qux = 2 + foo * bar / -2 + baz % ((age - 4));
    num++;
    num--;
    foo = baz has bar;
    foo = baz contains bar;
    foo = baz and bar or buzzy;

    doghouse = "dog" >< "house";

    fruits = ["apple", "pear"];

    fruits <+ "banana";
    "mango" +> fruits;

    // test operator precedence rules

    result = base >< person.getName();

    bar = assertions.passed + assertions.failed;

    if bar exists {
    }

    if foo undefined {
    }

    if foo defined {
    }
};