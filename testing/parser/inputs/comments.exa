// Line comment should be ignored

/* Block comment on one line should be ignored */

/*
 * Block comment
 * on multiple lines
 * should be ignored
 */

     // Leading whitespace before a comment should be ignored
     /* Leading whitespace before a comment should be ignored */


/*
 * Variations on comments inside a block to test block indent handling
 * and comment handling play nicely
 */

receive args, io;

max is 10;

num = 0;
while num < max:
//  no indent
    num++;

num = 0;
while num < max:
  // small indent
    num++;

num = 0;
while num < max:
    // right indent
    num++;

num = 0;
while num < max:
      // large indent
    num++;

num = 0;
while num < max:
	// tab indent
    num++;

num = 0;
while num < max:
    num++;
    num--;
//  no indent
    num++;

num = 0;
while num < max:
    num++;
    num--;
  // small indent
    num++;

num = 0;
while num < max:
    num++;
    num--;
    // right indent
    num++;

num = 0;
while num < max:
    num++;
    num--;
      // large indent
    num++;

num = 0;
while num < max:
    num++;
    num--;
	// tab indent
    num++;
