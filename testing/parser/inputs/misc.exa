// literals and assignments

foo = "hello";
foo = true;
foo = false;
foo = 10;

foo++;
foo--;

foo += 5;
foo -= 7;
foo *= 10;
foo /= 4;
foo %= 360;

doSomething();

foo[] = 17;

monkeys["chickens"][15].wow({ ready: true, height: 72, answer: 42 })[17](57);

block = connect("Block")();

/* comment */

monkeys = 72;

result = foo() catch:
    receive err;
    log(err);
    reply fixed;

// generator - the outer process is one entry point; it creates and returns *another* entry point
// getIterator

index = 0;

iterator is:
    reply items[index];
    index++;

reply iterator;
