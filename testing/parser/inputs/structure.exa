/**
 * Created by: spurcell
 * 11/10/13
 *
 * problem: can't tell if x -> y is an invocation (x is a constant, or non-action-valued variable)
 * or a sequence definition (x is an action; b = x -> y defines action b)
 */

receive num

// assign an action to x
x =
    pass

if # items == 0
    fail "oh noes!"

if # items == 1
    return "ok"

if answer == 42
    a -> b
else if answer == 57
    b -> c
else
    c -> d

return answer