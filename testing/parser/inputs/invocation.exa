// async by default

foo();           // where foo holds an action address
foo.bar();       // where bar is an action address on record foo

// promise form

x = foo();

// async

foo() => x;

// sync form in expression

foo(bar());

x = foo() + 7;

// todo we also want to support standalone try's (no assignment)
x = foo() ~>
    receive error;
    fail error;
